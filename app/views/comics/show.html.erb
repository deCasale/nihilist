<%= render 'navigation' %>
<div id="content">
	<% if @comic %>
	<h2 id="comic-title"><%= @comic.title %></h2>
	<div id="comic">
		<% if @comic.image %>
			<%= image_tag comic_image_path(@comic) %>
		<% end %>
	</div>
	<% else %>
	<h2 id="comic-title">Nothing here...</h2>
	<% end %>
</div>
